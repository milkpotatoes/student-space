var $ = mdui.$;
let d = document;
var template;

document.title = "请稍后...";

fetch("/json/template.json")
    .then(e => {
        return e.json();
    })
    .then(e => {
        template = e;
    });

$("body").addClass("mdui-theme-primary-green mdui-theme-accent-red");
let s = d.createElement("script");
s.setAttribute("src", "js/index" + (navigator.vendor === "Apple Computer, Inc." ? "" : "") + (location.host.includes("dev") ? "" : ".min") + ".js");
// "js/index" + (navigator.vendor == "Apple Computer, Inc." ? "_ios" : "") + (location.host.includes("dev") ? "" : ".min" + ".js")
d.body.appendChild(s);
if (navigator.userAgent.includes("Android")) {
    $(".widgets").removeClass("mdui-hidden");
}
if (location.host.includes("dev")) {
    /*网页调试工具*/
    /*eruda网页调试控制台*/
    var script = document.createElement('script');
    script.src = "dev/vconsole.min.js";
    document.body.appendChild(script);
    script.onload = () => {
        // eruda.init()
        var _onload = window["onload"];
        var $vConsole = new VConsole;
    };
    // let vs = d.createElement("script");
    // vs.setAttribute("src", "dev/vconsole.min.js");
    // d.body.appendChild(vs);
}
if((new Date()) >= new Date("2021/12/13 00:00") && (new Date()) <= new Date("2021/12/13 24:00")){
    document.querySelector("html").style.filter = "grayscale(1)";
    mdui.snackbar("沉痛悼念南京大屠杀遇难同胞");
    }

s = undefined;